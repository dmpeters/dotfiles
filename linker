#!/usr/bin/env bash
if [ -z "$PS1" ]; then
  echo -e "${COLOR_RED}You should source this, not run it, for example: source link${COLOR_NC}\n\n"
fi

rm ~/.bash_aliases
ln -s ~/Projects/dotfiles/aliases ~/.bash_aliases

source ~/.bashrc
echo ':: success ::'
